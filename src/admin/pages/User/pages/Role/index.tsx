import React, { FunctionComponent, useContext } from "react";
import "./index.scss";  

const Role: FunctionComponent = ({ children }) => {

  
  return (
    <div className={"Role"}>
      role
    </div>
  );
};

export default (Role);
